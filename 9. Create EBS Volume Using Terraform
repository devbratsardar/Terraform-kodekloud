provider "aws" {
  region = "us-east-1"
}

# Create EBS Volume
resource "aws_ebs_volume" "nautilus_volume" {
  availability_zone = "us-east-1a"
  size              = 2
  type              = "gp3"
  
  tags = {
    Name = "nautilus-volume"
  }
}
